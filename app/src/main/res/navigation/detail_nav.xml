<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/detail_nav"
    app:startDestination="@id/detailFragment">

    <fragment
        android:id="@+id/detailFragment"
        android:name="com.kospin.myapplication.fragment.DetailFragment"
        android:label="fragment_detail"
        tools:layout="@layout/fragment_detail" >
        <action
            android:id="@+id/klik_foto"
            app:destination="@id/detailFotoFragment" />
    </fragment>
    <fragment
        android:id="@+id/detailFotoFragment"
        android:name="com.kospin.myapplication.fragment.DetailFotoFragment"
        android:label="fragment_detail_foto"
        tools:layout="@layout/fragment_detail_foto" />
</navigation>